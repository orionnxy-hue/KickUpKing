// This component is intentionally empty as we are using HTML5 Canvas for high-performance rendering.
// Keeping the file structure clean for potential future React-based UI components related to the ball (e.g. customization).
import React from 'react';

export const BallSkin = () => null;